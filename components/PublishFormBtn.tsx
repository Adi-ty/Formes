import React from "react";
import { Button } from "./ui/button";

function PublishFormBtn() {
  return (
    <Button className="gap-2 text-white bg-gradient-to-r from-rose-400 to-teal-400">
      Publish
    </Button>
  );
}

export default PublishFormBtn;
